# Large source documents (record metadata in SOURCES.json; use Git LFS if you must commit)
/sources/*.pdf
/sources/*.epub
/sources/**/*.pdf
/sources/**/*.epub

# Optional: PDF placed at dataset root for a single-book workflow (omit from git or use LFS)
/*.pdf
/*.epub

# OS / editor noise
Thumbs.db
Desktop.ini
*.tmp
/extreme_degraded